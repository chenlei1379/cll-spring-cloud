server:
  port: 8761 #eureka的默认端口
eureka:
  client:
    register-with-eureka: false # 因为是单机，不要将eureka当成客户端，当前service就可以了
    fetch-registry: false # 因为是单机，不要将eureka当成客户端，当前service就可以了
    service-url:
      defaultZone: http://localhost:8761/eureka
  